#include<stdio.h>
int main (){
    float a;
    printf("Enter a real number : ");
    scanf("%f",&a);
    
    if (a>=0)
    printf("The absolute vakue is %f",a);
    if (a<=0)
    {a= a*(-1);
    printf("the absolute value is %f",a);}
return 0; 
    }
